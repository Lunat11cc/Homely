/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "avatars.yandex.net",
            "lh3.googleusercontent.com"
        ]
    }
};

export default nextConfig;
