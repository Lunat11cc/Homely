/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "avatars.yandex.net",
            "lh3.googleusercontent.com",
            "res.cloudinary.com"
        ]
    }
};

export default nextConfig;
